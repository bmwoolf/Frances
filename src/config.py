# config file for inputs as preparation for turning this into an API
config = {
    "host": "E.coli",
    "target": "limonene",
    "constraints": {"max_knockouts": 3}
}
host = config["host"]
target = config["target"]